const firstName = 'John';
const lastName = 'Doe';
const age = 30;

const person = {
  firstName,
  lastName,
  age,
}

// Destructuring

const todo = {
  id: 1,
  tittle: 'Take out trash',
}

const id = todo;

console.log(id);